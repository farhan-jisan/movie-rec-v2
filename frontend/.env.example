# Public base URL for the FastAPI backend.
# - In local dev, leave empty so Vite's `/api` proxy forwards to localhost:8000.
# - On Vercel, set this to your Render URL, e.g. https://mr-api.onrender.com
VITE_API_BASE=